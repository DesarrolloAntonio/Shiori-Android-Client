<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <!--
        allowBackup is off because the DataStore holds the session token (and, until that is
        reworked, the password). With it on, adb backup or a Google Drive backup copies those
        off the device.

        usesCleartextTraffic stays on deliberately. Shiori is self hosted and most users point the
        app at http://192.168.x.x or a .local name. Android's network security config can only
        name individual hosts, not address ranges, so there is no way to permit LAN cleartext
        while blocking it everywhere else at build time. Restricting this properly needs a runtime
        decision per server, not a manifest change.
    -->
    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:name=".ShioriApplication"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Shiori"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:theme="@style/Theme.Shiori"
            tools:ignore="DiscouragedApi">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <meta-data
                android:name="android.app.lib_name"
                android:value="" />
        </activity>
        <!--
            Its own task affinity. This activity used to finish itself in onPause so that it could
            not come back when the launcher icon was tapped, which also meant that pulling down the
            notification shade, taking a call or entering split screen threw away everything the
            user had typed. A separate task keeps it out of the main task without killing it.
        -->
        <activity android:name=".ui.bookmarkeditor.BookmarkEditorActivity"
            android:label="Save in Shiori"
            android:exported="true"
            android:taskAffinity="${applicationId}.share"
            tools:ignore="DiscouragedApi">
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/plain" />
            </intent-filter>
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>

    <queries>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <category android:name="android.intent.category.BROWSABLE" />
            <data android:scheme="https" />
        </intent>
    </queries>

</manifest>
